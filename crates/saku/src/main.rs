//! Saku binary entrypoint.
//!
//! `saku` runs the Discord bot; `saku login` will delegate to `saku-cli`.

fn main() {
    eprintln!("saku: not yet implemented — see GitHub issue #1");
    std::process::exit(1);
}
